# ESLint baseline — kataleya-flawers-web
#
# Snapshot de errores/warnings pre-existentes del proyecto. Debe estar vacío o casi.
# Regenerar con:  npm run lint:strict   (= eslint . --max-warnings 0)
#
# Nota: `.claude/**` se ignora en eslint.config.mjs (skills/commands locales,
# no trackeados por git, no son código del proyecto).

Estado actual: LIMPIO — 0 problemas (0 errors, 0 warnings).
