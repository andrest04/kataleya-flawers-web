export type CampaignMode = 'contact' | 'catalog';

export interface HeroSlideView {
  ctaExternal: boolean;
  ctaHref: string;
  ctaLabel: string;
  focus: string;
  imageAlt: string;
  imageSrc: string;
  kicker: string;
  title: string;
}
